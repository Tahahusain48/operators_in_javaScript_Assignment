//syntax for ternary operator
//condition ? expressionIfTrue : exprressionIfFalse
//In JavaScript, the ternary operator (also known as the conditional operator) is a concise way to write an if-else statement. It is often used to assign a value to a variable based on a condition. 
let number = 8;
let result = (number % 2 === 0) ? 'Even' : 'Odd';
console.log(result);
